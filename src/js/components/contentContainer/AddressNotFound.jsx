import React from 'react';

const AddressNotFound = () => (
	<div className='address-not-found'>
		Address does not found here!
	</div>
);

AddressNotFound.propTypes = {
};

export default AddressNotFound;